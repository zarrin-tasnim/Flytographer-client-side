import React from 'react';
import useTitle from './../../hook/useTitle';

const Review = () => {
    useTitle('Review');
    return (
        <div>
            
        </div>
    );
};

export default Review;