import React from 'react';
import useTitle from './../../hook/useTitle';

const Review = () => {
    useTitle('Review');
    return (
        <div>
           Review 
        </div>
    );
};

export default Review;